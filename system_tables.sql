select * from all_tables
where table_name = 'EMPLOYEES';

-- table column info
select * from all_tab_columns
where table_name = 'EMPLOYEES';

-- details about all the database objects like tables, index, sequence etc
select * from all_objects;

